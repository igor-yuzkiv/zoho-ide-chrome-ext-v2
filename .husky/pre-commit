#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🐘 PHP Pint (dirty only)"
cd backend || exit 1
vendor/bin/pint --dirty || exit 1
cd - >/dev/null

cd frontend || exit 1
echo "🔎 Typecheck"
npx nx affected -t typecheck || exit 1

echo "🔗 Lint"
npx nx affected -t lint || exit 1

echo "🎨 Prettier (write)"
npx nx format:write || exit 1